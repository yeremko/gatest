name: Debug PR Event
on:
  pull_request:
    types:
      - labeled
      - closed
jobs:
  debug_event:
    runs-on: ubuntu-latest
    steps:
      - name: Dump GitHub event payload
        run: echo "${{ toJSON(github.event) }}"
        shell: bash
